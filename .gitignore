# Claude file
CLAUDE.md

# env fleses
env
.env
.env.local

# Python build/test artifacts
__pycache__/
*.pyc
.pytest_cache/

# Model weights — downloaded on demand; see src/fitcoach/pose.py for the URL.
models/*.task

# Trained classifier checkpoints and training history (reproducible from
# scripts/train_model.py — not worth versioning).
models/*.pt
models/*.history.json

# C++ build artifacts (CMake + compiled .so).
cpp/build/